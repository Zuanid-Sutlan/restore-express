openapi: "3.0.3"
info:
  title: "Restore Express API"
  description: "API documentation for the Restore Express repair and shop platform."
  version: "1.0.0"
servers:
  - url: "http://localhost:8080"
paths:
  /api/health:
    get:
      summary: "Health Check"
      responses:
        '200':
          description: "OK"
components:
  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
